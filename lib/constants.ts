import { Dimensions } from "react-native";

export const rem = 16;
export const { width: vw, height: vh } = Dimensions.get("window");
export const btnPadding = 0.5 * rem;
export const inpPadding = 0.1 * rem;
